%li.row
  .desc= change_event.desc
  =link_to "/users/#{@user.id}/change_events/#{change_event.id}", :method => :delete, :remote => true, :class => 'delete btn btn-rounded btn-flat-danger btn-mini', :data => {:confirm => '是否删除?'} do
    %i.fa.fa-trash-o
    %span 删除