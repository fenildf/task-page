%li.row
  .desc= task_submit.desc
  =link_to "/users/#{@user.id}/task_submits/#{task_submit.id}", :method => :delete, :remote => true, :class => 'delete btn btn-rounded btn-flat-danger btn-mini', :data => {:confirm => '是否删除?'} do
    %i.fa.fa-trash-o
    %span 删除